package org.kg.ctl.mapper;

/**
 * Description:
 * Author: 李开广
 * Date: 2024/1/24 10:58 AM
 */
public interface TargetDbQueryMapper {
//    @Select("<script>select ${uniqueKey} from ${tableId} where  ${uniqueKey} in <foreach item='item' collection='list' open='(' separator=',' close=')'> #{item} </foreach></script>")
//    <T> List<T> selectList(@Param("list") List<T> otherDbList, @Param("tableId")String tableId, @Param("uniqueKey")String uniqueKey);
}
